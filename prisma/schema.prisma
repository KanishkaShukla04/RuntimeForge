generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Submission {
  id         String   @id @default(cuid())
  formName   String
  data       Json
  createdAt  DateTime @default(now())
}